import 'package:odo/features/practice/domain/entities/session.dart';

abstract final class StreakCalculator {
  // Stub — full implementation in Story 3.5.
  static int compute(List<Session> sessions, DateTime today) => 0;
}
